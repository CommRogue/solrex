job-manager:
  max-cached-jobs: 100
solr:
  networking-mode: "legacy"
  envs:
    dest: "localhost:2181"
    src: "localhost:2182"
spring:
  application:
    name: solrex-back
server:
  port: 8081
springdoc:
  default-property-example: false
  swagger-ui:
    use-root-path: true
logging:
  level:
    root: info